import React, { Fragment } from 'react';

const EmptyScreen = ({ showDays, currentDay, setCurrentDay, reloadDays, setScreen, deleteDay }) => {

  return (
    <div className="days__details">
      emptry
    </div>
  )
};

export default EmptyScreen;
